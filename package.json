{
  "name": "spasource",
  "version": "1.0.0",
  "private": true,
  "description": "SpaSource — the Med Spa Supply Sourcing Hub",
  "scripts": {
    "start": "serve . -l ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.6"
  },
  "engines": {
    "node": ">=18"
  }
}
